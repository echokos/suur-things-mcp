[project]
name = "suur-things-mcp"
dynamic = ["version"]
description = "A safe, fast MCP server for the Things 3 task manager (Cultured Code) on macOS — reads from the local SQLite database, writes only through the official Things URL Scheme."
readme = "README.md"
requires-python = ">=3.11,<3.12"
license = { text = "MIT" }
authors = [{ name = "Artyom Sklyarov" }]
keywords = ["mcp", "things", "things3", "cultured-code", "todo", "task-manager", "macos", "claude", "llm"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: MacOS X",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: MacOS",
    "Programming Language :: Python :: 3",
    "Topic :: Office/Business :: Scheduling",
]
dependencies = [
    "mcp>=1.2.0",
    "things.py>=0.0.15",
    # starlette + uvicorn ship transitively with mcp; declared explicitly
    # because the dashboard imports them directly.
    "starlette>=0.37",
    "uvicorn>=0.30",
]

[project.urls]
Homepage = "https://suur.io"
Source = "https://github.com/artyomsklyarov/suur-things-mcp"
Repository = "https://github.com/artyomsklyarov/suur-things-mcp"
Issues = "https://github.com/artyomsklyarov/suur-things-mcp/issues"

[project.scripts]
suur-things-mcp = "suur_things_mcp.server:main"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=8.0",
    # Real-browser tests for the dashboard's JS (quick-add guards, XSS escaping).
    # Tests skip cleanly when the Chromium binary isn't installed, so
    # `uv run pytest` still works without `playwright install`.
    "pytest-playwright>=0.5",
    "ruff>=0.8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# Single-source the version: __init__.py owns it; the build reads it from there
# (bumping two files in lockstep was a release footgun).
[tool.hatch.version]
path = "src/suur_things_mcp/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/suur_things_mcp"]

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
# Defaults (E4/E7/E9/F) + imports, bugbear, pyupgrade. E501 stays off: the
# dashboard/server keep some long annotated lines on purpose.
select = ["E4", "E7", "E9", "F", "I", "B", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "browser: real-Chromium tests (skipped if Playwright's browser isn't installed)",
]

[tool.ruff.lint.per-file-ignores]
# The test suite deliberately uses compact `x = ...; x.mkdir()` setup lines.
"tests/*" = ["E702"]
